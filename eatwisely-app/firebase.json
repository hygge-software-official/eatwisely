{
  "react-native": {
    "crashlytics_debug_enabled": true,
    "crashlytics_auto_collection_enabled": true,
    "analytics_auto_collection_enabled": true,
    "messaging_auto_init_enabled": true
  },
  "firestore": {
    "rules": "service cloud.firestore { match /databases/{database}/documents { match /{document=**} { allow read, write: if request.auth != null; } } }",
    "indexes": "{\"indexes\": [{\"collectionId\": \"your-collection\",\"fields\": [{\"fieldPath\": \"your-field\",\"mode\": \"ASCENDING\"},{\"fieldPath\": \"another-field\",\"mode\": \"DESCENDING\"}]}]}"
  },
  "storage": {
    "rules": "service firebase.storage { match /b/{bucket}/o { match /{allPaths=**} { allow read, write: if request.auth != null; } } }"
  }
}
